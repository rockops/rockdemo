# Built extension packages
*.vsix

# rockDemo per-run scratch (staged asset copies, bind-mounted into containers)
.rockdemo-run/

# Node
node_modules/

# Editor/OS noise
.DS_Store

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json
